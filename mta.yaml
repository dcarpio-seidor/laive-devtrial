_schema-version: "3.2"
ID: nslaivelaivedevtrial
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: nslaivelaivedevtrial-destination-content
  type: com.sap.application.content
  requires:
  - name: nslaivelaivedevtrial-destination-service
    parameters:
      content-target: true
  - name: nslaivelaivedevtrial-repo-host
    parameters:
      service-key:
        name: nslaivelaivedevtrial-repo-host-key
  - name: nslaivelaivedevtrial-uaa
    parameters:
      service-key:
        name: nslaivelaivedevtrial-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: nslaivelaivedevtrial_html_repo_host
          ServiceInstanceName: nslaivelaivedevtrial-html5-service
          ServiceKeyName: nslaivelaivedevtrial-repo-host-key
          sap.cloud.service: nslaivelaivedevtrial
        - Authentication: OAuth2UserTokenExchange
          Name: nslaivelaivedevtrial_uaa
          ServiceInstanceName: nslaivelaivedevtrial-xsuaa-service
          ServiceKeyName: nslaivelaivedevtrial-uaa-key
          sap.cloud.service: nslaivelaivedevtrial
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: nslaivelaivedevtrial-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: nslaivelaivedevtrial-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nslaivelaivedevtrial.zip
      name: nslaivelaivedevtrial
      target-path: resources/
- name: nslaivelaivedevtrial
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: nslaivelaivedevtrial-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: nslaivelaivedevtrial-destination-service
    service-plan: lite
- name: nslaivelaivedevtrial-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: nslaivelaivedevtrial-xsuaa-service
    service-plan: application
- name: nslaivelaivedevtrial-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: nslaivelaivedevtrial-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
